# AI-First SaaS Application Architecture

## Status and provenance

This is the canonical AI-first SaaS architecture doctrine for this skills pack.
It distills temporary migration concept material retained only as source-repository provenance.
Those archived source materials are not installed-pack guidance; this document is the authority for repository guidance and routing work.

## Default target architecture

Applications generated by this skills pack are **secure AI-first SaaS applications by default**. More specifically, they are **full-stack secure AI-first SaaS applications by default**. Use this target from the start of every new project, PRD, app-description, backlog, or generation request unless the user explicitly asks for repository-maintenance-only or non-SaaS reference material.

Security is mandatory. Security and the web UI are mandatory from the first planning step, never later hardening or polish phases, and never dependent on whether the user mentions them. Every generated SaaS design starts with a mandatory secure SaaS foundation plus browser UI surfaces for sign-in, context selection, administration, supervision, decisions, governance, audit/traces, and outcomes, then adds app-specific AI-first/domain behavior.

AI-first SaaS means the harness models products as:

```text
human objective
→ mandatory secure SaaS foundation
→ durable goal and plan
→ bounded agent or agent team execution
→ policy, permission, evidence, and approval controls
→ human supervision, decision, approval, teaching, audit, and exception handling
→ traceable outcomes and learning loops
```

Agents should do as much bounded work as the product safely permits. Humans retain ownership of outcomes and supervise, decide, approve, audit, teach, correct, and govern authority changes.

AI-first does not mean every app must use every AI-first pattern. It means every generated app begins with secure SaaS foundations, then applies only the agentic substrate objects, governance surfaces, and Akka components justified by product intent.

Existing Akka component skills remain substrate implementation tools. They should be selected after the secure SaaS foundation, AI-first operating model, object model, governance requirements, and UI surfaces are understood well enough to implement bounded components.

## What AI-first SaaS is not

Do not treat AI-first SaaS as:

- CRUD screens with generated text added later;
- a dashboard with a chatbot panel;
- a workflow builder where AI only drafts labels or summaries;
- prompt-only automation without durable goals, policies, permissions, or traces;
- an omnipotent assistant that can act without explicit authority boundaries.

Conversation can be an input and review channel, but consequential work should resolve into durable, inspectable objects and auditable actions.

## Mandatory secure SaaS foundation

The foundation is part of top-level product meaning and must be visible before component-specific routing or app-specific CRM/domain features.

Required baseline:

| Foundation area | Mandatory expectation |
|---|---|
| Identity | Authenticate humans through a defined identity seam, typically WorkOS/AuthKit for browser apps. Keep provider secrets out of frontend assets. |
| Local authorization | Maintain Akka-owned authorization state separate from authentication: `Account`, `UserProfile`, `UserSettings`, `Membership`, `Role`, `Permission`/`Capability`, and selected `AuthContext`. |
| Organization model | Model `SaaS Owner`, `Tenant`, and `Customer` boundaries. Tenants own application data; Customers are organizations served within a Tenant boundary. |
| Memberships and roles/scopes | Authorize by active membership, status, role/scope, tenant/customer ids, and capabilities. Do not authorize by email alone or frontend navigation state. |
| Email-invite onboarding | Complete onboarding through mandatory `Invitation` lifecycle: invite token or acceptance context, status, expiry, resend, revoke/cancel, acceptance, delivery status, delivery attempts, idempotency, and audit trail. Production readiness requires configured email delivery or an accepted provider decision; local/dev/test must use an explicit captured outbox adapter. |
| `/api/me` | Provide browser-safe current account, status, memberships, selected/default context, roles/capabilities, profile, and settings. First-login linking must require a valid invitation or accepted membership policy and must not silently self-register privileged users. |
| Backend authorization | Enforce scope mechanically on every protected route, component command, view query, stream, agent tool, workflow action, consumer side effect, and timer action. |
| Audit and traces | Record identity, role, membership, tenant/customer, policy, approval, data-access, tool-use, decision, and outcome events as durable audit/work traces. |
| Tenant isolation | Include tenant/customer ids in scoped records; filter all reads by authorized context; reject cross-tenant/customer commands and queries. |
| Security tests | Require tenant-isolation, forbidden-access, disabled-user, role/scope denial, `/api/me`, audit, frontend secret-boundary, and security-review tests. |
| Security review | Treat missing auth/security semantics as not-ready for generation, not as an assumption to fill in later. |

Use `core-ai-first-saas-foundation.md`, `core-saas-identity-tenancy-admin.md`, and `core-saas-owner-tenant-billing.md` as the product-agnostic baseline. App-specific requirements may extend this foundation but must not weaken it.

## Human operating roles

Model users by responsibility rather than only by account type. One person may hold several roles.

| Role | Responsibility |
|---|---|
| Intent Author | Defines goals, success criteria, constraints, preferences, thresholds, and definitions of done. |
| Supervisor | Monitors active goals, plans, agents, progress, exceptions, approvals, and material activity. |
| Reviewer / Approver | Decides high-impact, uncertain, risky, or policy-boundary cases. |
| Exception Handler | Resolves ambiguity, data gaps, authority limits, policy conflicts, and blocked actions. |
| Policy Owner / Coach | Turns corrections into durable policy, examples, thresholds, and governed behavior changes. |
| Auditor | Investigates what happened, who or what authorized it, which evidence and policies were used, and why. |
| Outcome Owner | Determines whether agent labor is creating business value at acceptable risk. |

## Durable substrate objects

AI-first applications still need domain objects, but they should also model the agentic substrate explicitly when relevant.

Core object categories:

- **Intent:** `Goal`, `Objective`, `SuccessCriterion`, `Constraint`, `PolicyDocument`, `PolicyClause`, `Guardrail`, `ReferenceExample`.
- **Execution:** `Agent`, `AgentTeam`, `AgentDefinition`, `AgentSkillVersion`, `ExecutionPlan`, `Workflow`, `Task`, `ToolInvocation`, `DataAccessEvent`, `WorkResult`.
- **Judgment:** `Recommendation`, `Decision`, `ApprovalRequest`, `Exception`, `Escalation`, `Counterproposal`, `Precedent`.
- **Evidence and risk:** `EvidenceItem`, `ConfidenceScore`, `RiskScore`, `ImpactEstimate`, `StakesEstimate`, `AlternativeConsidered`, `ReasoningFactor`.
- **Learning and governance:** `HumanFeedback`, `LearnedRule`, `SkillProposal`, `PolicyProposal`, `PolicyCommit`, `ReplayResult`, `SimulationResult`.
- **Accountability and outcomes:** `AuditEvent`, `WorkTrace`, `DecisionTrace`, `PolicyInvocation`, `OutcomeMetric`, `OutcomeLink`, `RollbackRecord`.

These objects should be durable where they affect behavior, authorization, explainability, learning, or audit.

## Required product surfaces

AI-first products may include tables, forms, and detail pages, but the primary UX should support delegation, supervision, judgment, teaching, catch-up, and audit.

| Surface | Purpose |
|---|---|
| Goal-to-Execution Workbench | Convert intent into a durable goal, plan, agent assignments, tool/data needs, approval gates, and launch controls. |
| Command Center / Mission Control | Show active objectives, agent activity, progress, risks, exceptions, approval queues, and material events. |
| Decision Card / Deviation Review | Present recommendation, evidence, risk, confidence, impact, policy trigger, alternatives, and action controls. |
| Policy / Governance / Learning Center | Version policies, prompts, skills, examples, thresholds, simulations, proposals, and human-authorized commits. |
| Async Digest / Executive Briefing | Compress routine activity while surfacing material events, pending decisions, outcomes, and trace links. |
| Audit / Work Trace | Explain who/what/when/why/how-authorized for actions, decisions, tool calls, data access, approvals, and outcomes. |

For secure foundation shape, start with `../docs/examples/ai-first-saas-seed-app-description/README.md`. For UI composition and implementation routing, use `../skills/ai-first-saas-ui-surfaces/SKILL.md` together with the existing Akka web UI skills and AI-first reference app-description examples such as `../docs/examples/ai-first-dca-app-description/`.

## Akka + React/Vite/TypeScript substrate

The implementation target is a full-stack system. Backend-only generation is not the default target for this pack:

- **Akka backend** for durable behavior, orchestration, events, projections, agent execution, APIs, streaming, and integration.
- **React + Vite + TypeScript frontend** for supervision, decision, governance, digest, trace, and conventional domain screens.

Map AI-first concepts onto Akka components deliberately:

- **Event Sourced Entities:** audit-grade state, decisions, policies, goals, traces, or domain records where event history and temporal reasoning matter.
- **Key Value Entities:** current-state objects where event history is not required.
- **Workflows:** long-running plans, approval gates, retries, compensations, pause/resume, and multi-step agent orchestration.
- **Agents:** bounded LLM responsibilities such as planning, classification, recommendation, summarization, tool use, evaluation, and explanation.
- **Views:** CQRS read models for command centers, decision queues, digests, policy lists, audit searches, and outcome dashboards.
- **Consumers:** event reactions, trace enrichment, notifications, downstream publication, and asynchronous integrations.
- **Timed Actions:** deadlines, reminders, periodic digests, expiry, rechecks, and scheduled governance/replay work.
- **HTTP/gRPC/MCP endpoints:** browser APIs, service contracts, streaming surfaces, and AI-client tools/resources/prompts.
- **Web UI skills:** React app structure, typed clients, forms, realtime updates, accessibility, and responsive supervision surfaces.

## Governance and authority rules

Treat prompts, skills, policies, rules, thresholds, approval gates, and permissions as runtime business logic:

- version them;
- record provenance;
- test and evaluate them;
- replay or simulate impactful changes;
- require human authorization before activating high-impact behavior changes;
- enforce permissions mechanically, not only through prompts.

Agents may recommend or draft governance changes, but policy commits and expanded authority should remain human-governed unless the product explicitly defines a safe autonomous governance boundary.

## Audit, trace, and outcome rules

Generated systems should create audit and trace records during execution rather than trying to reconstruct them only from logs.

Always capture foundation security events for identity, membership, role/scope, tenant/customer context, authorization denials, admin changes, and security-sensitive data access. Also capture AI-first work events for:

- goal, plan, agent, prompt/skill/policy versions;
- policy clause invocations and approval gates;
- evidence considered and alternatives rejected;
- tool invocations and data access;
- recommendations, decisions, overrides, and precedents;
- final actions, rollback status, and outcomes.

Outcome metrics should validate whether agent labor is useful, safe, timely, and improving. Outcome links should connect decisions and automation to later business results.

## Minimal checklist

Before calling a generated design AI-first, verify:

- [ ] Secure SaaS foundation is present: identity, local authorization, Tenant/Customer model, memberships, roles/scopes, mandatory email-invite onboarding, `/api/me`, backend authorization, audit, tenant-isolation tests, frontend secret-boundary tests, and security review.
- [ ] Goals are durable and include objective, owner, constraints, success criteria, and outcome links.
- [ ] Agents have explicit responsibilities, versioned prompts/skills, permissions, thresholds, escalation rules, and traces.
- [ ] Execution plans are inspectable, policy-bound, and auditable before or during activation.
- [ ] Policies and clauses are versioned, addressable, testable, and cited by actions or decisions.
- [ ] Approval and exception flows include evidence, risk, confidence, impact, alternatives, and learning options.
- [ ] Human corrections can become precedents, examples, policy proposals, or threshold changes through governed commits.
- [ ] Work traces include tool calls, data access, policy invocations, recommendations, approvals, and outcomes.
- [ ] Routine activity can be compressed for UX while remaining audit-complete.
- [ ] Replay or simulation exists for impactful prompt, skill, policy, threshold, or governance changes.
- [ ] Security, privacy, tenant isolation, redaction, retention, and permissions are mechanically enforced.
- [ ] Mandatory React/Vite/TypeScript UI surfaces cover foundation administration plus objectives, supervision, decisions, governance, digests, audit/traces, and outcomes over CRUD navigation by default.

## Routing implications for future tasks

When high-level product input arrives, route first through secure AI-first SaaS interpretation and the mandatory foundation unless the task is explicitly repository-maintenance-only or non-SaaS reference material. Start with `../skills/ai-first-saas/SKILL.md`, apply the foundation docs, then choose among description-first maintenance, Akka solution decomposition, PRD/spec/backlog planning, or focused implementation skills.

Use the existing AI-first companion skills only for concerns that are actually in scope:

- `../skills/ai-first-saas-object-model/SKILL.md` for durable goals, plans, policies, decisions, traces, outcomes, and related substrate objects.
- `../skills/ai-first-saas-agent-team-design/SKILL.md` for bounded coordinator/specialist/evaluator agent teams, authority limits, tools, escalation rules, traces, and workflow supervision.
- `../skills/ai-first-saas-policy-governance/SKILL.md` for policies, clauses, permissions, thresholds, approval gates, proposals, simulations, and human-governed commits.
- `../skills/ai-first-saas-decision-cards/SKILL.md` for recommendation, approval, exception, and deviation review surfaces with evidence, risk, confidence, impact, alternatives, and actions.
- `../skills/ai-first-saas-audit-trace/SKILL.md` for durable work, decision, policy, tool, data-access, approval, and outcome traces.
- `../skills/ai-first-saas-ui-surfaces/SKILL.md` for supervision, decision, governance, digest, goal-to-execution, and audit UI surfaces.
- `../skills/ai-first-saas-outcomes-metrics/SKILL.md` for outcome loops, metrics, decision/outcome links, feedback, replay, and validation surfaces.

These skills route to the existing Akka substrate implementation skills; they do not replace component-specific guidance for agents, workflows, entities, views, consumers, timed actions, endpoints, or web UI delivery.
