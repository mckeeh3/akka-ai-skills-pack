/**
 * Browser and service API endpoints that expose governed backend capabilities.
 *
 * <p>Use {@code ai.first.api.foundation.*} for reusable foundation APIs, {@code ai.first.api.coreapp.*}
 * for built-in core app workstreams, and {@code ai.first.api.business.<area>} for user-owned
 * business-specific endpoints.
 */
package ai.first.api;
