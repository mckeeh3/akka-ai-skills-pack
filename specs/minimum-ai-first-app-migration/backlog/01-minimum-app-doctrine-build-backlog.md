# Backlog 1: Minimum App Doctrine

## Items

### 1.1 Add canonical minimum app doctrine

Create `docs/minimum-ai-first-saas-app.md` or an equivalent canonical section that defines User Admin workstream v0, minimum starter readiness, full-core readiness, and app-specific readiness.

### 1.2 Integrate doctrine into AI-first and workstream architecture

Update the AI-first SaaS doctrine and agent workstream doctrine so minimum app requests route to bootstrap-only User Admin workstream v0, not a generic chatbot.

### 1.3 Define markdown_response surface contract

Update structured surface guidance to define `markdown_response` as the first/base renderable surface, including markdown payload, sanitized HTML rendering, trace links, accessibility, and tests.
