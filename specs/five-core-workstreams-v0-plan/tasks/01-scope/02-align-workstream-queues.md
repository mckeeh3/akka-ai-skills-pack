# Task: Align sibling workstream queues with shared contract

## Objective

Review and update the five sibling queues so each inherits the shared five-core v0 contract and remains independently executable.

## Done criteria

- Each sibling queue has a first runnable task and a terminal verification task.
- Workstream-specific agent-type choices are explicit and not forced.
