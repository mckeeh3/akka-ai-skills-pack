# Sprint 03: Core Agent Seeds and Docs

Goal: ensure all five core workstream agents are seeded and invoked through the same configuration-driven runtime, and update guidance so this is a mandatory generated-app feature.

Tasks:

1. Seed managed runtime profiles for My Account, User Admin, Audit/Trace, Governance/Policy, and Agent Admin.
2. Update starter docs and validation gates.
3. Update skills/doctrine to elevate AI-first managed agents as a primary core architecture pillar.

Acceptance:

- Each core functional agent has an active seeded AgentDefinition, prompt, compact skill/reference manifests where applicable, model config, and tool boundary.
- Starter documentation and pack guidance make the runtime substrate mandatory before Agent Admin editing features.
