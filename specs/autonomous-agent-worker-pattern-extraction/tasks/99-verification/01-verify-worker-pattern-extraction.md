# TASK-AAWPE-99-001: Verify worker pattern extraction

## Objective

Verify the pattern extraction mini-project reached its done state, or append bounded follow-up tasks plus a new terminal verification task.

## Required reads

- all mini-project artifacts
- edited docs/skills

## Required checks

- `git diff --check`
- focused `rg` for AutonomousAgent worker pattern, examples, provider fail-closed, no fake success, v3 events, attention, surfaces, and next-worker candidates

## Commit message

`autonomous-agent-pattern: verify extraction`
