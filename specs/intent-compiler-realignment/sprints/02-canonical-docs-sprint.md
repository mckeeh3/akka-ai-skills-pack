# Sprint 02: Canonical Intent Compiler Docs

## Objective

Create a concise active doc set that becomes the source of truth for CII, current intent, app-description intent graph, global definitions plus workstream bindings, and intent-to-code realization.

## Scope

- Promote `intent-compiler-working-note.md` into stable canonical docs or split it into focused docs.
- Define the current app-description directory/file structure.
- Define input normalization, change reconciliation, and generation routing in the new model.
- Preserve only useful legacy detail.

## Acceptance criteria

- New docs are short, authoritative, and cross-linked.
- Docs explicitly say current canonical intent is not a historical ledger.
- Docs define global reusable definitions and workstream-specific bindings.
