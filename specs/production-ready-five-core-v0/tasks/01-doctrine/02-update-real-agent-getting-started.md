# TASK-PRODV0-01-002: Update getting-started prompts for real model-backed workstream agents

Update user-facing getting-started prompts and template env guidance so users configure and validate real model-backed workstream agents. Missing provider config should block model invocation safely, not silently fall back.
