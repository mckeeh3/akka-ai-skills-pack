# TASK-PRODV0-01-001: Harden implementation completion doctrine against mock/demo runtime paths

Update doctrine and installed-pack guidance so runtime features are only complete when the real local Akka path works. Reject mock/demo/deterministic normal runtime behavior for workstream agents and protected capabilities.
