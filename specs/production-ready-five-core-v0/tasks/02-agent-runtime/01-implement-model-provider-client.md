# TASK-PRODV0-02-001: Implement backend model provider client boundary

Add backend-only model provider configuration and a real provider adapter, with test-only fakes isolated to tests. Protect provider secrets and fail closed when required provider config is absent.
