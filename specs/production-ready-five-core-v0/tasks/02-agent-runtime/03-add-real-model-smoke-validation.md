# TASK-PRODV0-02-003: Add real model provider integration smoke test and manual validation guard

Add an explicit optional provider smoke validation path that runs when provider env is configured and skips loudly/actionably otherwise. It must submit through backend workstream messaging and verify response/trace shape.
