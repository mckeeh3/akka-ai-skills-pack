# TASK-PRODV0-02-002: Wire five core workstream agents through governed prompt assembly and real model invocation

Replace normal deterministic message responses with real governed agent invocation for all five core workstreams. Resolve active behavior documents, invoke the configured model provider, and emit traces.
