# Sprint 03: Spec, Backlog, and Readiness Reconciliation

## Goal

Make active planning artifacts point at the reconstructed current-intent graph and identify runtime/code follow-up work caused by migration discoveries.

## Tasks

- Update active specs/readiness references that cite old app-description paths.
- Record implementation drift as bounded follow-up tasks or pending questions.
- Preserve one-task-per-fresh-context execution semantics for any new work.

## Done criteria

- Active planning references no longer require legacy `app-description/` paths as authority.
- Gaps discovered during migration are queued, blocked, deferred, or explicitly out of scope.
