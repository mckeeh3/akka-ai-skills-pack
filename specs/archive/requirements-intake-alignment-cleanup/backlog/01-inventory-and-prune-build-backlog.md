# Backlog 01: Inventory and Prune Criteria

## Goal

Create the source-of-truth cleanup inventory and criteria before editing active guidance.

## Suggested harness task breakdown

1. Audit active intake/planning content and write `content-inventory.md`.
2. Define prune/rewrite/demotion criteria in `prune-and-rewrite-criteria.md`.

## Dependencies

- Current conversation findings.
- Existing related realignment specs for context only.

## Required checks

- `git diff --check`
- Search proof that all high-priority files from `conversation-capture.md` are represented in the inventory.

## Acceptance criteria

- Future tasks know exactly which files to rewrite, remove, demote, or keep.
- Criteria explain when deleting is preferred over archiving.
