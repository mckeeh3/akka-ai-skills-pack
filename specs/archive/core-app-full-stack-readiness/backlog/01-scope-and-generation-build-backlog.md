# Build Backlog 01: Scope and Generation Path

## Items

1. Create a full-core realization map from the canonical core PRD.
2. Add or update readiness/generation checks so full core cannot omit User Admin, Agent Admin, invite onboarding, workstream UI, governed runtime agents, or security tests.
3. Harden planning/task materialization guidance so generated tasks are concrete by module and component family.
4. Create a golden-path example showing PRD → app-description → decomposition → module specs → backlog → tasks.

## Completion signal

A future harness can start from `10-canonical-core-app-prd.md` and produce precise implementation tasks for the full-core sequence without relying on tacit knowledge.
