# Backlog 03: Intake and Planning Flows

## Goal

Make PRD/spec/backlog/pending flows decompose both new and incremental inputs through workstream graphs and governed-tools.

## Suggested harness task breakdown

1. Update solution decomposition and PRD-to-specs planning.
2. Update revised-PRD, change-request, slice/backlog, backlog-to-pending, and task-brief flows.
3. Update pending question/task and do-next flows.
4. Update concise usage/planning docs.

## Required checks

- `git diff --check`
- Search edited skills for `surface graph`, `role-specific dashboard`, `governed-tool`, `browser-tool`, `agent-tool`, `internal workstream agent graph`, and `incremental`.
