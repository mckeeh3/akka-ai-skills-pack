# TASK-5WS-02-002: Add backend workstream message endpoint and markdown_response contract

Add a backend `POST /api/workstream/messages` starter contract that authorizes selected AuthContext and functional agent access, records/returns workstream request-response items, and returns a `markdown_response` surface. Start with a deterministic local/demo response seam if model-provider integration is not available in local tests.
