# Sprint 03: Remaining Foundation Guidance Alignment

## Objective

Align remaining foundation and planning guidance that still describes old mode-first UI settings or obsolete style option ids after the main web UI style/theme refresh.

## Scope

- `/api/me` and foundation user-settings guidance that still uses `preferredColorMode` or `uiMode` light/dark semantics.
- Pending-question guidance that still offers the obsolete `atlas-ops-supervisory-console` style id.
- Focused stale-language validation for the mini-project scope.

## Acceptance criteria

- Foundation docs and skills use named theme selection with the four initial theme ids where browser theme preference is discussed.
- Pending-question generation uses the canonical `ai-first-workstream-enterprise` option, not the obsolete Atlas style id.
- Remaining mode/runtime search hits are reviewed and are either removed or explicitly unrelated to browser theme preference.
- The queue includes a new terminal verification task after this sprint.
