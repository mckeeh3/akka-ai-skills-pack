# Backlog 3: Frontend Reference Migration

## Items

1. Replace route/page shell with workstream shell.
2. Replace generic nav routes with functional-agent rail entries.
3. Add deep-link mapping for selected functional agents and surfaces.
4. Connect fixture `/api/me`, functional-agent, workstream, surface, and capability action clients.
5. Retire or revise old seed screens.
6. Rebuild static resources only after source migration is complete.

## Acceptance

- `frontend/` visibly implements the canonical workstream UI, not a page-first console.
