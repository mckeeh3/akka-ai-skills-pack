# TASK-5WS-00-001: Create five-core workstream v0 migration queue

Create a migration planning scaffold for changing the initial starter target from User Admin-only v0 to five visible core v0 workstreams backed by `markdown_response`.

Requirements:
- Capture the conversation and decision rationale.
- Create a pending task queue with one-session, self-sufficient tasks.
- Require every task to make a focused git commit before being marked done.
- Include docs, template backend, template frontend, fixture/test, and validation tasks.
