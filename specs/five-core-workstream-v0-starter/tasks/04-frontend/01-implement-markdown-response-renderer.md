# TASK-5WS-02-001: Implement frontend markdown_response surface renderer

Implement `markdown_response` as a first-class starter frontend surface, including types, renderer, sanitization, states, trace/correlation rendering, and tests. The renderer must not fall back to raw JSON for normal `markdown_response` payloads.
