# WorkOS AuthKit public client id, e.g. client_01...
VITE_WORKOS_CLIENT_ID=

# Must exactly match a Redirect URI configured in the WorkOS dashboard.
# For the Akka-hosted local app, use this value and browse to http://localhost:9000
VITE_WORKOS_REDIRECT_URI=http://localhost:9000
