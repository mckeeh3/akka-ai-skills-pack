# Starter Specs Seed

This directory is reserved for scaffolded planning artifacts, acceptance checklists, and pending tasks for downstream app extension.

For minimum/starter/basic generated SaaS scopes, the first queued implementation target must be the five core v0 workstream set: My Account, User Admin, Agent Admin, Audit/Trace, and Governance/Policy. Each workstream starts with `markdown_response` v1, workstream log behavior, backend capability boundary, and audit/work trace substrate.

Planning artifacts should preserve the workstream graph contract: affected workstream, role-specific dashboard attention, human surface graph node/edge, internal workstream agent graph delegation/result when present, workstream expertise update, governed-tool id, qualified browser-tool/agent-tool/internal-tool exposure, AuthContext, audit/work trace, and runtime validation requirement.

The starter now includes a bounded in-app notification foundation for My Account: backend-owned notification projection/lifecycle/preferences, scoped AuthContext redaction, and a notification center surface. Treat email, SMS, mobile push, webhook, Slack/Teams, external delivery providers, delivery analytics, and broad notification-platform work as future governed delivery-channel tasks, not as implemented scaffold behavior.

Full-core follow-up tasks must remain visible and explicitly labeled until they are implemented and tested. Do not mark the scaffold as full-core ready just because the five core v0 workstreams are present.
