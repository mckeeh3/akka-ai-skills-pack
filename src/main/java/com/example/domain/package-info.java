/**
 * Pure domain code for the service.
 *
 * <p>This package contains domain models and focused helpers for the shopping cart and order
 * examples, including state, commands, events, validators, command handlers, business logic, and
 * event handlers.
 */
package com.example.domain;
