/**
 * Pure domain code for the service.
 *
 * <p>This package contains:
 *
 * <ul>
 *   <li>the state, commands, and events for the template</li>
 *   <li>a command handler that decides which events to emit</li>
 *   <li>a business logic class for non-trivial command decisions</li>
 *   <li>an event handler that applies events to state</li>
 *   <li>an optional validator for edge-facing callers</li>
 * </ul>
 */
package com.example.domain;
