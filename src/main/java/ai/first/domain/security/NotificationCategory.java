package ai.first.domain.security;

/** Browser-safe in-app notification categories. */
public enum NotificationCategory {
  ALL,
  ATTENTION_REQUIRED,
  DIGEST_READY,
  DIGEST_BLOCKED,
  WORKSTREAM_UPDATE,
  PROVIDER_READINESS,
  POLICY_OR_GOVERNANCE,
  AUDIT_OR_SECURITY,
  INVITATION_DELIVERY
}
