/**
 * Akka component implementations for entities, views, workflows, consumers, timed actions, and agents.
 *
 * <p>Use {@code ai.first.application.foundation.*} for reusable SaaS foundation components,
 * {@code ai.first.application.coreapp.*} for built-in core app workstreams, and
 * {@code ai.first.application.business.<area>} for user-owned business-specific application code.
 */
package ai.first.application;
