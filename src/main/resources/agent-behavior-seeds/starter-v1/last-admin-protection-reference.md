# Last Admin Protection Rule reference

Do not remove, suspend, disable, or downgrade the last active tenant administrator. Recovery should invite or assign another authorized admin before retrying the risky change. Backend command handlers enforce this invariant.

Confirmed chat tool plan reference: exact snapshot confirmation never weakens last-admin protection. Backend command handlers and approval policy still block any plan or surface action that would remove, disable, or downgrade the last active tenant administrator.
