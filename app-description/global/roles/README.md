# Global Roles

Reusable role and selected-`AuthContext` definitions for the core starter current-intent graph. Role names are descriptive; backend membership, selected context, explicit capability grants, support-access state, policy decisions, and governed-tool checks are authoritative.
