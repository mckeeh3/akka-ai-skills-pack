# Global Agents

Reusable functional-agent definitions for the core starter current-intent graph. These agents are AI-backed workers only when bound through `../workers/foundation-workers.md`, managed-agent behavior state, an active model policy/provider configuration, tool boundaries, trace obligations, and workstream-local tools/tests/realization evidence.
