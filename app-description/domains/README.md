# Domains

Application domains in the current-intent graph. The built-in secure SaaS core starter domain is `core-starter`; user-owned business domains should be added through extension zones rather than by replacing this domain.
