# Realization: API contracts for My Account

API contracts must expose capability-backed browser actions and queries for `account-context-and-profile` through JWT-protected, browser-safe endpoints. Responses include typed surfaces, safe denials, correlation ids, trace links, and no secrets.

Agent-tool and internal-tool exposures use the same capability authority and trace obligations as browser actions.
