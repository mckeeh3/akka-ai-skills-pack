# Agent Admin Trace bindings

Current authoritative Trace bindings for this workstream live in:

- [`work-traces.md`](work-traces.md)

This README is an index only; sibling files are the current-intent source of truth for this area.
